name: 'pr-workitem-creator'
description: 'this action will create a PR work item in my demo instance'
author: 'abelsquidhead'
inputs:
  myInput:              # change this
    description: 'input description here'
    default: 'default value if applicable'
runs:
  using: 'node12'
  main: 'dist/index.js'
